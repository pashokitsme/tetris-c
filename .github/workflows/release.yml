name: release

on:
  push:
    branches: [ "release" ]

jobs:
  build:
    runs-on: windows-latest
    steps:
    - name: checkout
      uses: actions/checkout@v3

    - name: build
      run: build.bat

    - name: release
      uses: marvinpinto/action-automatic-releases@v1.2.1